package com.chess.api.model.movement.condition;

public enum Location {
    LAST_MOVED,
    AT_START,
    AT_DESTINATION,
    AT_COORDINATE,
    PATH_TO_DESTINATION,
    PATH_TO_COORDINATE,
    BELOW_DESTINATION,
}
